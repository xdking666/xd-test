# 🏠 New Tab Extension Starter

基于 **Vue 3 + TypeScript + Vite** 的新标签页浏览器扩展脚手架，支持 Chrome、Edge、360、QQ 等 Chromium 内核浏览器。

## ✨ 特性

- 🔥 **Vue 3** - 最新的 Vue 3 Composition API
- 📦 **Vite** - 极速的构建工具，支持热更新
- 🎯 **TypeScript** - 完整的类型支持
- 📝 **Manifest V3** - 最新的扩展规范
- 🗂️ **Pinia** - Vue 3 官方状态管理
- 🌍 **跨浏览器** - 支持所有 Chromium 内核浏览器
- 🎨 **现代 UI** - 精美的深色主题界面

## 🖼️ 功能预览

- ⏰ **时间显示** - 实时时钟与日期
- 🔍 **搜索框** - 支持百度/Google/Bing 切换
- 🔗 **快捷链接** - 常用网站快速访问
- ⚙️ **个性化设置** - 自定义背景图片等

## 🌐 支持的浏览器

| 浏览器     | 支持状态    |
| ---------- | ----------- |
| Chrome     | ✅ 完全支持 |
| Edge       | ✅ 完全支持 |
| 360 浏览器 | ✅ 完全支持 |
| QQ 浏览器  | ✅ 完全支持 |

## 📁 项目结构

```
new-tab-extension/
├── public/
│   └── assets/                # 静态资源（图标等）
├── scripts/
│   ├── vite.config.ts         # Vite 构建配置
│   └── gulpfile.mjs           # 热更新逻辑
├── src/
│   ├── background/
│   │   └── main.ts            # 后台服务脚本
│   ├── newtab/                # 🆕 新标签页
│   │   ├── index.html
│   │   ├── main.ts
│   │   └── App.vue            # 新标签页主组件
│   ├── popup/                 # 扩展弹出设置页
│   │   ├── index.html
│   │   ├── main.ts
│   │   └── App.vue
│   └── tools/
├── manifest.json              # 扩展配置文件
└── package.json
```

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

### 生产构建

```bash
npm run build
```

## 📦 加载扩展

### Chrome

1. 打开 `chrome://extensions`
2. 开启"开发者模式"
3. 点击"加载已解压的扩展程序"
4. 选择 `dist` 文件夹
5. 打开新标签页即可看到效果！

### Edge / 360 / QQ 浏览器

步骤相同，在各自的扩展管理页面加载 `dist` 文件夹即可。

## ⚙️ 配置说明

### manifest.json 关键配置

```json
{
    "chrome_url_overrides": {
        "newtab": "src/newtab/index.html" // 覆盖新标签页
    },
    "permissions": [
        "storage", // 保存设置
        "topSites", // 获取常访问网站
        "bookmarks", // 获取书签（可选）
        "favicon" // 获取网站图标
    ]
}
```

### 权限说明

| 权限        | 用途                               |
| ----------- | ---------------------------------- |
| `storage`   | 保存用户设置（背景图、搜索引擎等） |
| `topSites`  | 获取用户最常访问的网站作为快捷链接 |
| `bookmarks` | 可选，用于显示书签                 |
| `favicon`   | 获取网站图标显示在快捷链接上       |

## 🎨 自定义开发

### 修改默认主题

编辑 `src/newtab/App.vue` 中的样式：

```css
.newtab-container {
    /* 修改背景渐变色 */
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
}
```

### 添加新的搜索引擎

在 `src/newtab/App.vue` 中修改 `searchEngines` 数组：

```typescript
const searchEngines = [
    { name: 'baidu', label: '百度', url: 'https://www.baidu.com/s?wd=' },
    { name: 'google', label: 'Google', url: 'https://www.google.com/search?q=' },
    { name: 'bing', label: 'Bing', url: 'https://www.bing.com/search?q=' },
    // 添加更多搜索引擎
    { name: 'sogou', label: '搜狗', url: 'https://www.sogou.com/web?query=' },
];
```

### 添加天气、待办等功能

可以在 `src/newtab/App.vue` 中添加新的区域：

```vue
<template>
    <div class="newtab-container">
        <!-- 现有内容 -->

        <!-- 添加天气组件 -->
        <WeatherWidget />

        <!-- 添加待办事项 -->
        <TodoList />
    </div>
</template>
```

## 🏪 商店上架

### 打包扩展

```bash
# Windows PowerShell
Compress-Archive -Path dist\* -DestinationPath extension.zip
```

### 上架链接

- [Chrome Web Store](https://chrome.google.com/webstore/devconsole)
- [Edge Add-ons](https://partner.microsoft.com/dashboard/microsoftedge/overview)
- [360 扩展中心](https://ext.se.360.cn/)
- [QQ 浏览器开放平台](https://browser.qq.com/extensions/)

## ⚠️ 注意事项

1. **新标签页权限**：覆盖新标签页需要用户明确同意
2. **性能优化**：新标签页加载频繁，注意性能优化
3. **离线支持**：考虑添加 Service Worker 实现离线功能
4. **数据同步**：可使用 `chrome.storage.sync` 实现跨设备同步

## 📄 许可证

MIT License
